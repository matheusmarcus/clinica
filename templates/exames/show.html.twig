{% extends 'base.html.twig' %}

{% block title %}Exames{% endblock %}

{% block body %}
    <h1>Exames</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exame.id }}</td>
            </tr>
            <tr>
                <th>Data</th>
                <td>{{ exame.data ? exame.data|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Imagens</th>
                <td>{{ exame.imagens }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('exames_index') }}">Voltar à Lista</a>

    <a href="{{ path('exames_edit', {'id': exame.id}) }}">edit</a>

    {{ include('exames/_delete_form.html.twig') }}
{% endblock %}
