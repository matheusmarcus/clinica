{{ form_start(form) }}
<h5 class="card-title">Dados de Acesso</h5>
<div class="form-row">
    <div class="col-md-6">{{ form_row(form.usuario) }}</div>
    <div class="col-md-6">{{ form_row(form.password) }}</div>
</div>

<div class="form-row">
    <div class="col-md-6">{{ form_row(form.perfil) }}</div>
    <div class="col-md-6">{{ form_row(form.roles) }}</div>
</div>


    {{ form_widget(form) }}
<hr>
<a class="btn btn-secondary mr-2 mb-2" href="{{ path('acesso_index') }}">Voltar à Lista</a>
<button class="btn btn-success mr-2 mb-2">{{ button_label|default('Salvar') }}</button>
{{ form_end(form) }}
