{% extends 'base.html.twig' %}

{% block title %}Prontuarios{% endblock %}

{% block body %}
    <h1>Prontuarios</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prontuario.id }}</td>
            </tr>
            <tr>
                <th>DsProntuario</th>
                <td>{{ prontuario.dsProntuario }}</td>
            </tr>
            <tr>
                <th>Data</th>
                <td>{{ prontuario.data ? prontuario.data|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('prontuarios_index') }}">Voltar à Lista</a>

    <a href="{{ path('prontuarios_edit', {'id': prontuario.id}) }}">edit</a>

    {{ include('prontuarios/_delete_form.html.twig') }}
{% endblock %}
