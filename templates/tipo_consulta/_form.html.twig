{{ form_start(form) }}
    {{ form_widget(form) }}
<hr>
    <a class="btn btn-secondary" href="{{ path('tipo_consulta_index') }}">Voltar à Lista</a>
    <button class="btn btn-success">{{ button_label|default('Salvar') }}</button>
{% if app.request.pathinfo != '/tipo/consulta/new' %}
    {{ include('tipo_consulta/_delete_form.html.twig') }}
{% endif %}
{{ form_end(form) }}
