{% extends 'base.html.twig' %}

{#{% block title %}Edit TipoConsulta{% endblock %}#}

{% block body %}
    {#    <h1>Edit TipoConsulta</h1>#}
    <div class="page-header">
        <h1>
            <span class="text-muted font-weight-light">Tipo de Sessão / </span>Cadastrar Tipo de Sessão
        </h1>
    </div>
    <div class="main-card mb-3 card">
        <div class="card-body">
            {{ include('tipo_consulta/_form.html.twig', {'button_label': 'Atualizar'}) }}
        </div>
    </div>
    {#    <a href="{{ path('tipo_consulta_index') }}">Voltar à Lista</a>#}

{#    {{ include('tipo_consulta/_delete_form.html.twig') }}#}
{% endblock %}
