{% extends 'base.html.twig' %}

{% block title %}TipoConsulta{% endblock %}

{% block body %}
    <h1>TipoConsulta</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>IdtipoConsulta</th>
                <td>{{ tipo_consultum.idtipoConsulta }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ tipo_consultum.nome }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ tipo_consultum.descricao }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_consulta_index') }}">Voltar à Lista</a>

    <a href="{{ path('tipo_consulta_edit', {'idtipoConsulta': tipo_consultum.idtipoConsulta}) }}">edit</a>

    {{ include('tipo_consulta/_delete_form.html.twig') }}
{% endblock %}
