{% extends 'base.html.twig' %}

{#{% block title %}Edit Receitas{% endblock %}#}

{% block body %}
    {#    <h1>Edit Receitas</h1>#}
    <div class="page-header">
        <h1>
            <span class="text-muted font-weight-light">Receita / </span>Editar Receita
        </h1>
    </div>
    <div class="main-card mb-3 card">
        <div class="card-body">
            {{ include('receitas/_form.html.twig', {'button_label': 'Atualizar'}) }}
        </div>
    </div>
{#    <a href="{{ path('receitas_index') }}">Voltar à Lista</a>#}

    {{ include('receitas/_delete_form.html.twig') }}
{% endblock %}
