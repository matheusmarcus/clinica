{% extends 'base.html.twig' %}

{#{% block title %}Receitas{% endblock %}#}

{% block body %}
    {#    <h1>Receitas</h1>#}
    <div class="page-header">
        <h1>
            <span class="text-muted font-weight-light">Receita / </span>Visualizar Receita
        </h1>
    </div>
    <div class="main-card mb-3 card">
        <div class="card-body">
            <table class="table table-striped">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ receita.id }}</td>
                </tr>
                <tr>
                    <th>Descrição</th>
                    <td>{{ receita.dsReceita }}</td>
                </tr>
                <tr>
                    <th>Paciente</th>
                    <td>{{ receita.pacientespacientes }}</td>
                </tr>
                </tbody>
            </table>
            <hr>
            <a class="btn btn-secondary" href="{{ path('receitas_index') }}">Voltar à Lista</a>

            <a class="btn btn-primary" href="{{ path('receitas_edit', {'id': receita.id}) }}">Editar</a>
        </div>
    </div>
    {#    {{ include('receitas/_delete_form.html.twig') }}#}
{% endblock %}
